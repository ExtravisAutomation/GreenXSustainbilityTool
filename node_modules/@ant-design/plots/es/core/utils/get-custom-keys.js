import { TRANSFORM_OPTION_KEY } from '../constants';
import { getShapeConfigKeys } from './get-shape-config-keys';
export var getCustomKeys = function () {
    var customKeys = [];
    Object.keys(TRANSFORM_OPTION_KEY).forEach(function (key) {
        customKeys.push.apply(customKeys, Object.keys(TRANSFORM_OPTION_KEY[key]));
    });
    var configKeys = getShapeConfigKeys();
    customKeys.push.apply(customKeys, configKeys);
    return customKeys;
};
