import { CHART_OPTIONS } from '../constants';
/**
 * 统一删除不消费的配置项
 */
export var deleteChartOptionKeys = function (options) {
    Object.keys(options).forEach(function (key) {
        if (CHART_OPTIONS.includes(key)) {
            delete options[key];
        }
    });
    return options;
};
