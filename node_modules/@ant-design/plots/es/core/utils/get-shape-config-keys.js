import { CONFIG_SHAPE } from '../constants';
export var getShapeConfigKeys = function () {
    var configKeys = [];
    CONFIG_SHAPE.forEach(function (item) {
        configKeys.push(item.key);
    });
    return configKeys;
};
